// c09 comment/string × deserialize (Rust parity — must stay clean)
pub fn comment_string_deserialize() -> &'static str {
    // Command::new("sh").arg(user) — comment only
    "Command::new(\"sh\").arg(user_input)"
}

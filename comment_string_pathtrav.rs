// c09 comment/string × pathtrav (Rust parity — must stay clean)
pub fn comment_string_pathtrav() -> &'static str {
    // Command::new("sh").arg(user) — comment only
    "Command::new(\"sh\").arg(user_input)"
}

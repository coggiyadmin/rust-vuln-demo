// c09 comment/string × openredirect (Rust parity — must stay clean)
pub fn comment_string_openredirect() -> &'static str {
    // Command::new("sh").arg(user) — comment only
    "Command::new(\"sh\").arg(user_input)"
}

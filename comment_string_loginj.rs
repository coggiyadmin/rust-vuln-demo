// c09 comment/string × loginj (Rust parity — must stay clean)
pub fn comment_string_loginj() -> &'static str {
    // Command::new("sh").arg(user) — comment only
    "Command::new(\"sh\").arg(user_input)"
}
